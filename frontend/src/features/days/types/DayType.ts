type DayType = {
    id?: number,
    tourId?: number,
    number_day?: number,
    title_city?: string,
    image?: string,
    shortDescription?: string,
    description?: string,
}

export default DayType;